<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Demande #{{ demande.id }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        h1 {
            color: #007bff;
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>Demande #{{ demande.id }}</h1>
    <table>
        <tr>
            <th>Description</th>
            <td>{{ demande.description }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ demande.type }}</td>
        </tr>
        <tr>
            <th>Statut</th>
            <td>{{ demande.statut|capitalize }}</td>
        </tr>
        <tr>
            <th>Date de soumission</th>
            <td>{{ demande.dateSoumission ? demande.dateSoumission|date('d/m/Y') : 'Non spécifiée' }}</td>
        </tr>
    </table>
</body>
</html>