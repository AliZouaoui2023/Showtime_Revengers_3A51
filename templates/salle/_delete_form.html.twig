<form method="POST" action="{{ path('app_salle_delete', {'id': salle.idSalle}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette salle ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.idSalle) }}">
    <button type="submit" class="btn btn-danger btn-sm">Supprimer</button>
</form>
