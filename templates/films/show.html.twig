{% extends 'Back/base.html.twig' %}

{% block title %}🎬 Détails du Film{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card bg-dark text-light p-4 shadow-lg">
        <h2 class="text-center mb-4">🎬 {{ film.NomFilm }}</h2>

        <div class="table-responsive">
            <table class="table table-hover table-dark rounded">
                <tbody>
                    <tr>
                        <th>ID</th>
                        <td>{{ film.id }}</td>
                    </tr>
                    <tr>
                        <th>Nom du Film</th>
                        <td>{{ film.NomFilm }}</td>
                    </tr>
                    <tr>
                        <th>Date de Production</th>
                        <td>{{ film.DateProduction ? film.DateProduction|date('Y-m-d') : 'Non spécifiée' }}</td>
                    </tr>
                    <tr>
                        <th>Réalisateur</th>
                        <td>{{ film.Realisateur }}</td>
                    </tr>
                    <tr>
                        <th>Genre</th>
                        <td>{{ film.Genre }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="d-flex justify-content-between mt-3">
            <a href="{{ path('app_films_index') }}" class="btn btn-secondary">🔙 Retour</a>
            <a href="{{ path('app_films_edit', {'id': film.id}) }}" class="btn btn-primary">✏️ Modifier</a>
        </div>
    </div>
</div>
{% endblock %}
