{% extends 'Back/base.html.twig' %}

{% block body %}
<div class="container mt-4">
    <div class="card bg-dark text-light p-4 shadow-lg">
        <h2 class="text-center mb-4">📝 Modifier un Film</h2>

        <div class="card-body">
            {{ include('films/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <div class="d-flex justify-content-between mt-3">
                <a href="{{ path('app_films_index') }}" class="btn btn-secondary">🔙 Retour</a>
                
                {{ include('films/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
