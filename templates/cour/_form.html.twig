{{ form_start(form) }}
    <div class="form-group">
        {{ form_label(form.typeCour) }}
        {{ form_widget(form.typeCour, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.cout) }}
        {{ form_widget(form.cout, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.dateDebut) }}
        {{ form_widget(form.dateDebut, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.dateFin) }}
        {{ form_widget(form.dateFin, {'attr': {'class': 'form-control'}}) }}
    </div>

    <button class="btn btn-primary mr-2">{{ button_label|default('Enregistrer') }}</button>
    
{{ form_end(form) }}